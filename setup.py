from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='We are beginning to become familiar with NoSQL databases',
    author='Jesus Santana',
    license='MIT',
)
